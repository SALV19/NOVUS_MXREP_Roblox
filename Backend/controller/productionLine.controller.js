const ProductionLine = require("../models/productionLine.model")

function productionLine(data) {
  console.log(data)
  // ProductionLine.createProductionLines(data);
}

module.exports = {
  productionLine
}